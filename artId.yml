config:
  target: 'http://13.57.81.229'
  phases:
    - duration: 10
      arrivalRate: 1
  defaults:
    headers:
  processor: "./load_testing/randomId.js"

scenarios:
  - flow:
    - loop:
      - function: "randomId"
      - get:
          url: "/main/reviews/ssr/{{business_id}}"
      count: 1